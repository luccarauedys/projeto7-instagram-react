import Stories from './Stories';
import Posts from './Posts';

const Content = () => {
  return (
    <section className="esquerda">
      <Stories />
      <Posts />
    </section>
  );
};

export default Content;
